/** 首页大标题「Yu」顶边 — SiteHeaderChrome 与首页 Hero 共用 */
export const HERO_TITLE_TOP_SENTINEL_ID = "hero-title-top-sentinel";

/** Blog / Mesh Lab 等面包屑首行顶边 — 与首页同一套 rootMargin 判定 Nav 底边线 */
export const NAV_BORDER_SENTINEL_ID = "nav-border-sentinel";
